//! Build script for acton-ai.
//!
//! Previously compiled Hyperlight guest binaries. After the ProcessSandbox
//! migration there is no build-time work — the subprocess re-execs the host
//! binary itself, so no separate guest artifacts exist.

fn main() {}
